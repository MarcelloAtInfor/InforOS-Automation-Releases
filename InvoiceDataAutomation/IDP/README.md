# IDP Model Export

Place the IDP Document Processor Flow export here:

- `CSI_APInvoice_Extract` — invoice OCR extraction model

Export from: IDP UI > Document Processor Flows > select flow > Export
